<!DOCTYPE html>
<html lang="en">
<head>
    {{> head}}
    <title>Змінити користувача</title>
</head>
<body>
    {{> adminHeader}}
    <main>
        <div class="container">
            <div class="justify-content-center">
				<div class="text-center">
					<h1 class="display-3 col-sm-12">Змінити користувача</h1>
                </div>
                <div class="row">
                    <div class="col-sm-12">
                        <form action="/users/update" method="POST">
                            <input type="hidden" name="id" value="{{user._id}}"/>
                            <div class="form-group">
                                <label for="firstName">Ім'я</label>
                                <input type="text" class="form-control {{#if errors.firstName}}is-invalid{{/if}}"
                                    id="firstName" name="firstName" value="{{user.firstName}}" maxlength="30"
                                    placeholder="Введіть ім'я" required>
                                <div class="invalid-feedback">
                                    {{errors.firstName.msg}}
                                </div>
                            </div>
                            <div class="form-group">
                                <label for="lastName">Прізвище</label>
                                <input type="text" class="form-control {{#if errors.lastName}}is-invalid{{/if}}"
                                    id="lastName" name="lastName" value="{{user.lastName}}" maxlength="30"
                                    placeholder="Введіть прізвище" required/>
                                <div class="invalid-feedback">
                                    {{errors.lastName.msg}}
                                </div>
                            </div>
                            <div class="form-group">
                                <label for="birthday">День народження</label>
                                <input type="date" class="form-control {{#if errors.birthday}}is-invalid{{/if}}"
                                    id="birthday" name="birthday" value="{{formatDate user.birthday 'yyyy-mm-dd'}}"
                                    placeholder="Введіть день народження" required/>
                                <div class="invalid-feedback">
                                    {{errors.birthday.msg}}
                                </div>
                            </div>
                            <div class="form-group">
                                <label for="address">Адреса</label>
                                <input type="text" class="form-control {{#if errors.address}}is-invalid{{/if}}"
                                    id="address" name="address" value="{{user.address}}" maxlength="100"
                                    placeholder="Введіть адресу" required>
                                <div class="invalid-feedback">
                                    {{errors.address.msg}}
                                </div>
                            </div>
                            <div class="form-group">
                                <label for="phone">Телефон</label>
                                <input type="tel" class="form-control {{#if errors.phone}}is-invalid{{/if}}"
                                    id="phone" name="phone" value="{{user.phone}}" maxlength="20"
                                    placeholder="Введіть телефон" required>
                                <div class="invalid-feedback">
                                    {{errors.phone.msg}}
                                </div>
                            </div>
                            <div class="form-group">
                                <label for="email">E-mail</label>
                                <input type="email" class="form-control {{#if errors.email}}is-invalid{{/if}}"
                                    id="email" name="email" value="{{user.email}}" maxlength="30"
                                    placeholder="Введіть e-mail" required>
                                <div class="invalid-feedback">
                                    {{errors.email.msg}}
                                </div>
                            </div>
                            <div class="form-group">
                                <label for="password">Пароль</label>
                                <input type="password" class="form-control {{#if errors.password}}is-invalid{{/if}}"
                                    id="password" name="password" value="{{user.password}}" minlength="6" maxlength="30"
                                    placeholder="Введіть пароль" required>
                                <div class="invalid-feedback">
                                    {{errors.password.msg}}
                                </div>
                            </div>
                            <input type="submit" class="btn btn-success" value="Зберегти"/>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </main>
    {{> footer}}
</body>
</html>